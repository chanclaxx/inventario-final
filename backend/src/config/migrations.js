const runMigrations = async () => {
  // Las migraciones se aplican manualmente.
  // Agrega aquí cualquier migración puntual si lo necesitas.
  console.log('✅ Migraciones: sin pendientes.');
};
 
module.exports = { runMigrations };